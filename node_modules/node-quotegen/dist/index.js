"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getQuote = void 0;
const data_1 = require("./data");
const exception_1 = require("./exception");
function getrandom(arr) {
    const randomIndex = Math.floor(Math.random() * arr.length);
    return arr[randomIndex];
}
function get_quotes(category = "all") {
    const categoryLower = category.toLowerCase();
    if (data_1.allquotes.hasOwnProperty(categoryLower)) {
        return data_1.allquotes[categoryLower];
    }
    else {
        throw new exception_1.CategoryNotFoundError("This Category Is Not Available");
    }
}
function getQuote(category = "all") {
    const quotes = get_quotes(category.toLowerCase());
    const single = getrandom(quotes);
    return single;
}
exports.getQuote = getQuote;
